// The one thing this panel draws for itself: a line saying why it cannot show
// anything better. CLAUDE.md SS5.4 -- a board that spent a year in a drawer
// must be able to tell you why it is not working.
#pragma once

void epaperSay(const char* headline, const char* detail);
